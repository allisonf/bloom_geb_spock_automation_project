import spock.lang.*
import geb.*
import geb.spock.*

class PartialRegisterSpec extends GebReportingSpec {

    // Change the name of the test to be more descriptive
    def "Register Test"() {
        // Replace function body
        when:
        println "Running Register Test in PartailRegisterSpec"

        then:
        assert true
    }

}
