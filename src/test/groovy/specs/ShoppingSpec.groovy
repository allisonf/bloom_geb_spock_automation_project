package specs

import spock.lang.*
import geb.*
import geb.spock.*

class ShoppingSpec extends GebReportingSpec {

    def "Add Items to Shopping Cart Test"() {
        // Replace function body
        when:
        println "Running add items to shopping cart test in ShoppingSpec"

        then:
        assert true
    }

    def "Remove Items From Shopping Cart Test"() {
        // Replace function body
        when:
        println "Running remove items from shopping cart test in ShoppingSpec"

        then:
        assert true
    }
}
