import spock.lang.*
import geb.*
import geb.spock.*

class PriceCombinationSpec extends GebReportingSpec {

    // Change the name of the test to be more descriptive
    def "Register Test"() {
        // Replace function body
        when: "Running the test"
        println "Running the register test in PriceCombinationSpec"

        then:
        assert true
    }

}
