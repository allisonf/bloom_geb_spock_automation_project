import spock.lang.*
import geb.*
import geb.spock.*

class RegisterSpec extends GebReportingSpec {

    // Replace test name with something more descriptive
    def "Register test"() {
        // Replace function body
        when: "Running the register test"
        println "Running the register test in RegisterSpec"

        then:
        assert true
    }

}
